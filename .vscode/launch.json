{
  "configurations": [
    {
      "name": "Docker Node.js Launch",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug",
      "platform": "node",
      "node": {
        "package": "${workspaceFolder}/project-catwalk/package.json",
        "localRoot": "${workspaceFolder}/project-catwalk"
      }
    }
  ]
}